# 威脅模型

狀態：**Native Full-Trust 目標規格；取代舊 workspace-jail 威脅模型**

依據：`OWNER_DECISION_FULL_CONTROL.md`、ADR-028

## 1. 範圍與安全目標

Orchestratory 在單一使用者的 Mac 上協調一或多個原生 TUI Agent。Native Full-Trust Agent 以登入
使用者的既有權限執行，能存取 filesystem、shell、Git、network、plugin 與 subagent。系統的主要
安全目標不是限制 Agent 能力，而是：

1. 讓多 Agent 的身分、傳訊、進度與候選成果可追溯；
2. 將日常任務成果預設保存在 candidate，不與 canonical main 混在一起；
3. 在任務終點以精確預覽與使用者核准決定是否 merge main；
4. 對誤刪、錯誤 merge、main drift 與崩潰提供偵測及復原資料；
5. 不洩漏 provider secrets 或把外部副作用混入 main merge 授權。

每小時 supervisor 只提供偵測與導正建議。它的失敗行為是以非零 exit code、bounded JSON report
與 launchd stderr 告警停止，不嘗試自動修復；因此仍需由明確、snapshot-bound 的 candidate/main
流程處理 drift。launchd 本身若被同一 macOS 使用者停用或繞過，屬於 Native Full-Trust 的同帳號
繞過風險，不宣稱強制監控。SQLite quick/foreign-key 與 Room hash-chain 稽核不等於 HMAC audit chain
或各 store 的完整 semantic integrity；後兩者仍由正式 gate 負責，督導不讀密鑰來擴大宣稱。

## 2. 非安全保證

同一 macOS 帳號下擁有完整能力的 Agent，可以繞過 Orchestratory，直接修改 main、刪除 candidate／
備份或停止監控。Candidate 因此不是不可突破的 OS sandbox；Native Full-Trust 是 recovery-first 的
協作模式，而非針對惡意同帳號程式的強隔離。

若產品需要強制 main 保護，必須另用不同 OS 身分、root-owned helper、唯讀 volume 或外部不可變
備份，並建立獨立模式及 threat model。

## 3. 主要資產

1. Canonical main 的 source、Git refs、working tree、hooks、mode 與使用者既有 dirty state。
2. Candidate branches、commits、untracked files、checkpoint 與 task metadata。
3. Provider session、API secrets、SSH keys、cookies 與其他本機敏感資料。
4. Room Ledger、exact-seat identity、inbox/thread、approval 與 audit integrity。
5. Recovery snapshots、Time Machine／外部備份狀態與 rollback 資訊。
6. 使用者的其他檔案與專案，以及 CPU、磁碟、網路、訂閱／API 額度。
7. 發布、push、deployment、package registry 與 GitHub 權限。

## 4. 信任角色

- **Owner**：唯一能核准 candidate → main merge 及外部副作用的人。
- **Native Full-Trust terminal seat**：有完整主機能力；在協作協議上不受信任，但預期遵守 main
  boundary 提示規範。
- **GUI Managed worker**：依 Owner 選擇受 bounded policy 控制。
- **Orchestratory control plane**：負責身份、ledger、thread、candidate metadata、preview、approval、
  promotion 與 audit；其資料與判斷仍可能有 bug。
- **Repository／dependency／外部內容**：不可信，可能包含 prompt injection 或惡意程式。
- **同帳號其他程序**：能與 Full-Trust Agent 一樣修改應用資料；不可假設為隔離。

## 5. 高優先級濫用案例

| ID | 濫用案例 | 主要控制 | 殘餘風險 |
|---|---|---|---|
| F01 | Agent 未經詢問直接修改 main | candidate-first prompt、canonical path 標記、main drift monitor、audit、recovery point | 同帳號完整權限可繞過或停止監控；只能 best-effort 偵測／復原 |
| F02 | 舊 approval 被 replay 到不同 candidate/main | single-use nonce、task/path/HEAD/preview digest binding、expiry | Owner 或同帳號程序若能改 approval store，仍可破壞應用層可信度 |
| F03 | 詢問後 candidate 或 main 改變 | confirm-time HEAD、tree、dirty-state、inode/fingerprint 重驗 | 重驗與 Git 操作間仍可能有同帳號 TOCTOU |
| F04 | Merge 隱含新刪除、rename 或 conflict resolution | 完整 preview、risk summary、scope expansion stop、重新核准 | Git／diff 分析 bug 可能漏報；需可恢復備份 |
| F05 | 兩個 Agent 覆蓋彼此工作 | per-agent branch/candidate、thread coordination、Git conflict detection、checkpoint | Agent 可選擇共享同一 working tree，仍可能產生非 Git 覆蓋 |
| F06 | Exact-seat 訊息被偽造成 Owner／其他 Agent，或第三席插入既有 thread | authenticated presence、source server-side binding、Room/workspace、reply-to participant/task binding、server-generated thread root、no fallback | MCP host 或本機 identity store 被同帳號篡改後不可強保證 |
| F07 | 指定 terminal 工作被改送常駐 worker，或 source/target unregister race 留下幽靈 queue／無人 await 的工作 | exact target ID、no-fallback invariant、presence delete-first、ledger/enqueue 前後雙端 reconciliation、delivery receipt、UI provenance | Terminal offline 時工作只能 failed／cancelled／拒絕，不能保證即時完成；跨 store 線性化仍靠 reconciliation 而非單一交易 |
| F08 | 固定回合 ceiling 中斷協作 | thread 無 round ceiling、transport wait 可重連、explicit cancel | 無限活躍 thread 仍可能耗用儲存／注意力，需 retention 而非對話上限 |
| F09 | Repo prompt 要求讀取或外傳 secrets | 系統規範、host 原生批准、secret redaction、audit minimization | Full-Trust Agent 技術上能讀取／傳送；Orchestratory 無法強制阻止 |
| F10 | Candidate 或 backup 被誤刪 | checkpoint、Git object verification、separate recovery copy、cleanup approval | 同帳號 Agent 可刪除所有可寫副本；外部備份才有較強恢復性 |
| F11 | Main merge approval 被誤當作 push／publish 核准 | action-specific approval、UI separation、audit scope | Full-Trust Agent 可自行執行外部命令；應由 host 原生批准與規範補強 |
| F12 | 惡意測試／dependency 破壞主機 | candidate、backup、host 原生 permission prompts、optional managed test mode | Native Full-Trust 執行任意 repo 程式即是主機 code execution |
| F13 | Room／log 洩漏 source、個資或 token | bounded schema、redaction、minimal retention、no raw reasoning/session data | Redaction 無法辨識所有秘密；Agent 可在訊息中主動貼出 |
| F14 | Web 頁面控制 localhost GUI | loopback、Host/Origin/CSRF/session checks、CSP | 瀏覽器、extension 或同帳號程序遭入侵 |
| F15 | Candidate mutation 回應遺失後被盲目重送 | 三個 mutation 均要求 stable UUID `clientRequestId`；`candidate_requests` 以 `client_request_id` 為主鍵 reserve-execute-settle，同 key 同 operation/room/digest 從 durable state 重建同一結果、不同則 fail closed；識別碼於 reserve 時鑄造、checkpoint ref 已存在且同 head 即採納，使 crash 與重連後的重試收斂；同一把 key 由記憶體內鎖確保同時只有一個執行中（併發回 `CANDIDATE_REQUEST_IN_FLIGHT`），`succeeded` 為終局，且 settle 與 discard 都以 owner token CAS，因此陳舊的席位無法覆寫或刪除現任持有者的判決（先前僅 discard 帶 token，settle 沒有，已實測可讓一個邏輯請求產出兩份 durable 成果）；已判定 `failed` 回 `CANDIDATE_REQUEST_FAILED_RETRY_WITH_NEW_KEY` 且換新 key 必有前進路徑；未產生 durable artifact 的中止會刪除保留；canonical main 不被修改 | seat 身分刻意不入 key（重連會重鑄 display name），因此同 Room 內取得他人 `clientRequestId` 者可觸發一次 replay 讀到同一份結果——Room 成員資格即授權邊界，且 replay 無法變更狀態；記憶體鎖只涵蓋單一程序，而每個 MCP seat 是獨立 OS process 共用同一 data directory：跨 process 的同 key 併發改由 **reservation 的不透明 `owner_token`** 擋下——採用既有保留時重鑄 token，原建立者中止時的 discard CAS 不再匹配，無法刪掉他人正在使用的保留。token 不從時鐘推導（時間戳在同一毫秒內不變，曾使該保護失效）；輸家會收到錯誤（可能是下游 UNIQUE 的原始訊息而非穩定碼）而非排隊。暫時性 Git 失敗**不再燒掉該 key**：終局 `failed` 改由 allowlist `DETERMINATE_REQUEST_FAILURES`（7 個確定性錯誤碼）決定，其餘一律視為「結果未知」而讓該列維持 `pending`，同一把 key 在環境恢復後可重試收斂。方向仍是 fail-closed——不 settle 不等於當作成功，動作照樣被拒絕，改變的只有 key 是否還活著。**新的殘餘風險**：確定性但未列入 allowlist 的失敗會讓該列永久停在 `pending`（由 `inventory().requestsPending` 曝露），caller 每次重試都收到同一個真實錯誤——以可見的重複錯誤換掉不可逆的誤燒。孤兒 recovery ref 現有唯讀出口 `orchestrator candidates orphan-refs <workspace>`（只列不刪，過 workspace allowlist）。`CANDIDATE_REQUEST_RECOVERING` 的等待由 `CREATING_RECOVERY_GRACE_MS`（5 分鐘）而非記憶體鎖決定。row-hash 為完整性保護非簽章，無法防禦同帳號的惡意寫入 |
| F20 | 半建立的 candidate 被誤判為「無人擁有」而遭回收，或被誤判為「有人擁有」而永遠不解析 | `candidate_requests` 記錄 `owner_pid`；`reconcileCreating` 只跳過擁有者仍存活的 `operation='start'` 保留（同程序、`kill(pid,0)` 成功、或 `EPERM`）。擁有者可證明已死時不守護該列，由既有的 worktree 證據解析；**不寫入帳本**，因此同一把 key 的重試仍能收斂到它自己保留所擁有的成果。守衛只涵蓋 `start` 保留，`checkpoint`／`complete` 的 pending 保留不受影響 | **pid 不是持久身分，兩個方向都會錯。誤判「還活著」**：pid 重用（macOS 約 99999 後回繞；**重開機後 pid 從低號重新開始，而重開機正是最可能造成保留被遺棄的原因**）、zombie 程序未被回收 → 該列被永久守護、`inventory()` 仍計為 active、同 key 永遠回 `CANDIDATE_REQUEST_RECOVERING`。**誤判「已死」**：資料目錄放在共享或同步磁碟上時，他機的 pid 在本機無意義；不同 pid namespace（容器）；`process.kill` 拋出 `ESRCH`／`EPERM` 以外的錯誤 → 可能回收仍在執行的建立者，其 transition CAS 失敗、key 被燒掉、依文件鑄新 key 即產生第二個 candidate。**現況接受此不確定性**：兩個方向的最壞結果都不會把錯誤內容寫進 canonical main，且都在 Owner 可見的 `candidate_status`／`inventory()` 中留下痕跡。另有一個**與 pid 正確性無關**的失效曾存在並已修復：寬限期內的重試會 adopt 保留並蓋上自己的 pid，接著回 `CANDIDATE_REQUEST_RECOVERING` 而不推進；由於 MCP seat 是長壽程序，該列會被守護到程序結束、同一把 key 永遠無法收斂。現行實作在 bail 前把原 pid 寫回（`#restoreOwnerPid`），因此寬限過後同一把 key 會收斂到原本的 candidate。**兩個方向若必須擇一，本設計偏好「誤判還活著」（永不解析、Owner 可在 `candidate_status` 與 `inventory()` 看見並手動處理）而非「誤判已死」（可能回收仍在執行的建立者並產生第二份成果）**——前者可見且可逆，後者會靜默增生 durable 成果。要完全消除須改用持久身分（pid ＋ process start time ＋ boot id）或保留期間持有的 lock file |
| F23 | Merge preview 依 repository 自身設定執行任意命令 | `candidate_complete` 的 `merge-tree` 預覽會套用 `.gitattributes` 指定的 merge driver，而 driver 定義是 `.git/config` 中的一段 shell 字串，因此 preview 會以 owner 的 uid spawn `/bin/sh -c <字串>`。緩解：driver 定義**只能**來自 repo 自身設定——`minimalGitEnvironment` 設 `GIT_CONFIG_NOSYSTEM=1`、`GIT_CONFIG_GLOBAL=/dev/null`、`core.hooksPath=/dev/null`，故 global／system 設定與 hooks 均被抑制；受管制的 Workspace MCP 寫入路徑拒絕任何含 `.git` 的路徑（`workspace-server.ts:36`），因此無法經由該路徑植入 driver | 單靠 `.gitattributes` 不足以觸發（未定義的 driver 名會退回內建 text merge），需要寫入 `.git/config`。Native Full-Trust seat 依設計本來就有 shell，因此**這不是權限提升**，但誠實的陳述是：**preview 會繼承 repository 自己的設定叫 git 執行什麼**。若日後要對不可信 repository 做 preview，必須另行加上 `-c merge.<driver>.driver=` 覆寫或改用不套用 driver 的比較方式。**2026-08-09 第十五輪補一句本列從未說過、而且改變本列裁決前提的事實**：本列把信任錨定在「driver **定義**只能來自 repo 自身設定」（要寫 `.git/config`，只有 Native Full-Trust seat 做得到），但**被執行的那個檔案不必是 repo 自己的**。實測（`m1-preview-driver.mjs`）：main 自己合法設定 `merge.x.driver = ./tools/m.sh`，`tools/m.sh` 是 tracked content，candidate worktree 內它是 **Writer 寫的版本**，而 `#mergePreview` 的 `git merge-tree --write-tree` cwd 就在 candidate worktree——於是 **Writer 的程式以 Owner 身分、無沙箱執行**，`id -un` 印出 Owner，**而且第一次執行發生在 `complete()` 裡，早於任何 preview 請求、完全沒有 Owner 參與**。`tools/m.sh` 不是 `.git`、也不是 hook 目錄，所以受管制的 Workspace MCP 寫入路徑**允許**它——本列「無法經由該路徑植入 driver」只對**定義**成立，對**腳本**不成立。因此對只有 MCP、沒有 shell 的 Writer 而言，這**是**權限提升，而本列原本的「這不是權限提升」只在 Native seat 的前提下成立。**本輪沒有修**（改法會改變 preview 語意並影響既有 driver 測試，屬於獨立一輪的工作）；已鎖進測試 `a merge driver script this merge rewrites runs as main's during promotion and as the candidate's during preview`，該測試斷言的是**現況**而不是可接受性。已量到的修法方向與本列原文相同（`-c merge.<name>.driver=`），並已實測其副作用：空值與 `false` 都讓 driver **不執行**，git 改回報衝突（exit 1、conflicted entries），亦即 `mergeable: false` → 該 repo 的 preview 一律不可核准（fail-closed，但也擋掉合法用途） |
| F24 | Merge 核准被重放、套用到別的 snapshot，或在綁定值改變後仍生效 | 核准是 schema v4 `candidate_merge_approvals` 的一列，至少綁 `taskId`／`completionId`／`roomId`／`mainPath`／`mainBranch`／`candidatePath`／`baseMainHead`／`candidateHead`／`mainHead`／main dirty 與 ignored fingerprint／`recoveryRef`／`previewDigest`，並在**建立、核准、消耗三個時點各驗一次**；任一值改變即回 `MAIN_MERGE_APPROVAL_BINDING_CHANGED:<欄位名>` 並轉為終局 `invalidated`。Single-use 由 `state`＋`row_hash` 的 compare-and-set 保證。Token 為 32 bytes 隨機值，只在 `approved` 期間以 SHA-256 存在，且不寫入 audit／ledger。**2026-08-14 更正：** ~~`promoteMainMerge` 目前沒有任何 HTTP 出口。~~ 本機 Owner 的 POST 現在在 server 內 grant＋立即 promotion，raw token 不再回到瀏覽器；response loss 重送只回讀 `approval_id` 已有的 durable promotion，不再跑 Git。前端只有在 row=`applied` 且 observation=`authorizedMergeCommit:true` 才顯示成功；其他狀態進 Merge 歷史並具名。 | 同帳號程序仍可直接改 store；row-hash 只讓竄改可偵測且不可用。SQLite 與 Git 不可原子提交，grant 後、intent 前若程序被 SIGKILL，仍可能留下短效 `approved` row 且 raw token 已不存在；它不能被使用，TTL 後失效，但這段期間 UI 必須如實顯示 terminal approval 而不是成功。正常 catch path 會以 `PROMOTION_NOT_STARTED_AFTER_GRANT` 退休。intent 之後的 crash 由 F27／ADR-035 的唯讀 reconciliation 處理。 |
| F25 | 核准存活期間發生漂移，卻在每一條讀取路徑上仍顯示為 `approved`，或失效得無聲無息 | 每一條 approval 讀取路徑（`candidate_status`、approval 列表、`inspect`，以及重新提出請求時）在回報那一列之前先對 live state 重驗綁定；漂移者在被回報前就已持久轉成終局 `invalidated`，`refusal` 帶改變的欄位名與偵測它的介面，並以 compare-and-set 保證恰好一筆稽核事件與一則 Room ledger 訊息（帳本為公開面，只列欄位名，不含路徑／id／token；owner-only audit 另記 `ownerHadGranted`，使「核准過但漂移作廢」與「從未核准」可區分）。**綁定檢查逐欄位獨立探測**：讀到且值不同才進 `changed`，讀不到一律進 `unverified`，例外絕不轉成「已改變」。`changed` 為空而 `unverified` 非空時 approval **不**失效、`token_hash` **不**清除、**不寫任何列**，只回 `bindingCheck.unavailable = MAIN_MERGE_APPROVAL_BINDING_CHECK_FAILED` 加 `unverified` 欄位名；環境恢復後下一次觀察即回報有效且仍可 consume。`grant`／`consume` 遇此情形以獨立錯誤型別`MergeApprovalBindingUnverifiableError`（`MAIN_MERGE_APPROVAL_BINDING_CHECK_FAILED:<欄位名>`）拒絕該次動作，但不轉為任何終局狀態。真的刪掉 recovery ref 仍算漂移（`rev-parse --verify --quiet` 的 exit 1 = ref 不存在，其餘 = 讀不到）。`test/merge-approval-drift.test.ts` 以 `chmod 000`、worktree 改名離開再放回、PATH 內無 git 三種真實失敗驗證，並斷言恢復後仍可成功 consume | 偵測是觀察時觸發，沒有背景輪詢：沒有人讀就沒有人發現，最終仍靠 grant／consume 各自重驗一次。讀取路徑因此會寫入——唯一可能的轉移是把已無法使用的核准記成終局失效（fail-closed 方向，無法授權、復活或刪除任何東西），但 `GET /api/rooms/merge-approvals/inspect` 在 CSRF 意義上不再是純讀取。**2026-08-06 更正**：本列原本寫「`mainIgnoredFingerprint` 仍只涵蓋 ignored 檔案的路徑而非內容」——5-5 已把它升級為**涵蓋內容**（`GitRestorePoint.ignoredFingerprint`），並在消耗核准前對 live main 重驗一次；observation 路徑上的節流快取仍只覆蓋 scalar 綁定值，hook 環境與 ignored 內容指紋由 `#authorizeMainMerge` 不節流地重讀（見 F26）。該 GET 端點目前**沒有 Origin／Referer 檢查**，實際緩解只有 `HttpOnly; SameSite=Strict` session cookie（`src/ui/web.ts:371`）、Host pinning（`src/ui/web.ts:345-346`，不符回 421）與 loopback-only 來源檢查（`src/ui/web.ts:341-343`）；放寬其中任何一項都會讓它變成可被跨站觸發的寫入。另：暫時失敗改為不失效之後，**大 repo 上 dialog 每 5 秒重算完整 preview 撞上 30 秒 deadline** 會讓觀察持續回 `unavailable`、confirm 一直不可用（不再永久失效，但仍是 5-5 前必須處理的成本問題，見 [[DECISIONS]] ADR-034 殘餘風險表）。此外，紀錄不再宣告 candidate／checkpoint／recovery ref 目前是否存在，只宣告「這次失效沒有刪除任何東西」；要知道現況必須另行查詢 |
| F26 | Promotion 以 Owner 身分、無沙箱執行 repo `.git` 內設定的 hook 與 merge driver，且 candidate 的 linked worktree 與 main 共用同一個 common `.git` | 這是 5-5 引入的新信任邊界，刻意不隱藏：**真實 merge 會執行 Owner 設定的 hook，preview 永遠不會**（所有唯讀 Git 指令固定 `core.hooksPath=/dev/null`，只有 promotion 用 `promotionGitEnvironment()`）。本次會執行的 hook 檔名與 SHA-256、`core.hooksPath`、`merge.*.driver` 與 `filter.*` 設定全部納入 `previewDigest`，因此也納入 approval 綁定，並在核准畫面上逐項列出；消耗核准前再比對一次 hook 指紋，不符即 `MAIN_MERGE_APPROVAL_BINDING_CHANGED:hookEnvironment` 且不執行任何東西。`filter.*.clean/smudge`（含 LFS）一律**偵測到即拒絕**；attributes 側的判準見下方 2026-08-06 第二次更正。**2026-08-06 更正**：本列原本寫「`.gitattributes` 內的 `filter=`」，而實作只讀了 **root** 的那一份——實測 `sub/deep/.gitattributes filter=lfs`、`.git/info/attributes`、被 ignore 的 `.gitattributes` 與 `core.attributesFile` 四種寫法全部漏掉，`approvable` 仍為 `true`。現在掃描的來源是：`git ls-files -- '*.gitattributes'`（**任意層級**，預設 pathspec glob 會跨 `/`，已實測）、ignored inventory 內任何 `.gitattributes`、`$GIT_DIR/info/attributes`、以及 repo-local `core.attributesFile`；數量／大小超限或讀不到一律 `MAIN_ATTRIBUTES_UNREADABLE`（讀不到不是「沒有 filter」）。**2026-08-06 第二次更正（第二輪審查）**：上一句的「任何一份 attributes 檔」是**假宣稱**——實測 `git check-attr` 在產品的確切環境下又找出兩個漏掉的來源：(a) `core.attributesFile` 寫成 `~/attrs` 時 git 用 `expand_user_path` 展開，而產品把它 `join(workspace, ...)` 成 `<main>/~/attrs` → ENOENT → 零 blocker；(b) 完全不設 `core.attributesFile` 時 git 仍讀 `$XDG_CONFIG_HOME/git/attributes`，`GIT_CONFIG_GLOBAL=/dev/null` 只覆蓋全域 **config** 檔、不覆蓋全域 **attributes** 檔（已用 `git check-attr` 證實）。修法不是再加一個檔案位置，而是**直接問 git**：以 `git check-attr -z --stdin filter` 在 `promotionGitEnvironment()`（就是 merge 會用的那個環境）下詢問一組代表性路徑＋本 repo 全部 tracked 路徑＋ignored 路徑，任何答案不是 `unspecified`／`unset` 即拒絕。列舉那半邊**保留**，因為它能抓到「規則指向此刻不存在的路徑」（例如 repo 內還沒有 `.psd` 時的 `*.psd filter=lfs`），而 check-attr 那半邊能抓到「這份程式碼不知道怎麼找到的 attributes 檔」。**兩半各自都不完備，合起來仍不宣稱完備**：一份全域 attributes 檔若其 pattern 既不匹配本 repo 任何路徑、也不匹配代表性清單，兩邊都看不到——這一條列在 [[VERIFICATION]] 殘餘風險表。**2026-08-06 第三次更正（第三輪審查）**：本列上一版寫「hook 檔名與 SHA-256、`core.hooksPath`、`merge.*.driver` 與 `filter.*` 設定全部納入 `previewDigest`」——那是一份**四個鍵的清單**，而 git 會執行的「由設定指定的程式」不只這四個。實測：candidate worktree 在**核准之後**寫入 `commit.gpgsign=true` ＋ `gpg.program=<script>`，`git merge --no-ff` 建立 merge commit 時**以 Owner 身分執行了那個 script**，而綁定、揭露、消耗前重驗三道全部看不到它；對照組 `core.hooksPath` 與 `merge.evil.driver` 則被正確拒絕，證明缺口是特定的而非全面。現在是兩個機制並用，**兩者都不宣稱窮盡任何清單**：(a) `promotionGitEnvironment()` 以 `GIT_CONFIG_KEY_n`（command-line 優先序，蓋過 `.git/config`）**釘死** `core.fsmonitor=false`、`commit.gpgsign=false`、`tag.gpgsign=false`、`merge.verifySignatures=false`——因此 **promotion 產生的 merge commit 不簽章、也不驗證被合併方的簽章**，這是明寫的取捨，不是遺漏；(b) main 在 promotion 環境下的**整份 effective config**（`git config --list -z` 的原始輸出，含所有這份程式碼沒聽過的鍵與其值）雜湊成 `hookEnvironment().configDigest`，納入 `previewDigest`、納入綁定、消耗前重驗，**這一半不是清單**，所以核准後新增的任何設定鍵都會變成 `MAIN_MERGE_APPROVAL_BINDING_CHANGED` 而不執行。揭露側另有 `programs`：main config 內**可指名程式**的鍵，逐一列在核准畫面上，**只列鍵名不列值**（`credential.helper` 之類的值可能夾帶秘密，值由 digest 覆蓋而不顯示）。**2026-08-07 第五次更正（第五輪審查）**：上一句只想到「值」。git 的鍵是 `section.subsection.name`，而 `credential.<url>.helper` 的 subsection **依規格就是一個 URL**，URL 可以帶 userinfo——瀏覽器實測 `credential.https://x-access-token:<token>@github.com.helper` 的 token **逐字渲染在核准畫面上**，而且也已經寫進 SQLite 的 `preview_json`。現在 `redactConfigSubsection()` 在鍵離開 `hookEnvironment()` 之前就把這類 subsection 換成 `<redacted>`（`credential.*` 一律，加上任何 subsection 含 `://` 或 `@` 的鍵），所以**那一個鍵**不再上畫面也不再進資料庫（這句原本寫的是「所以秘密既不上畫面也不進資料庫」，那是全稱宣稱且已被證偽，見下方第六次更正）；`merge.<name>.driver` 這類「subsection 是 Owner 取的名字」不動。`configDigest` 仍取自 git 的原始輸出，因此綁定完全不受遮蔽影響；`programs` 的比對式**明確不宣稱完整**，它只決定「哪些鍵會被逐項顯示」。**2026-08-07 第六次更正（第六輪審查）**：上一段的「秘密既不上畫面也不進資料庫」在寫下的當下就是假的，而且是 [[PITFALLS]] #108 在同一輪原地復發——`redactConfigSubsection()` 只接到 `programs`，同一個函式裡三行之外的 `drivers` 與 `filters` 整條繞過。實測（真實 `git config` 寫入合成 token）：`drivers` 得到 `merge.https://u:<token>@github.com.driver=true %O %A %B`、`filters` 得到 `filter.https://x-access-token:<token>@github.com.clean=cat`，兩者都**逐字**渲染在核准畫面上（`public/room.js:3005-3006`），也**逐字**以位元組存在 `candidate-registry.sqlite`。現在 `drivers` 與 `filters` 的**鍵名**走同一個 `redactConfigSubsection()`。**值仍然逐字顯示**，這是刻意的：對這兩份清單而言值就是 git 會執行的指令，遮蔽它等於拿掉揭露的全部意義。因此本列現在的宣稱限定為：**這三份清單的鍵名不會攜帶 URL 形狀的秘密上畫面或入庫；值不在此保證範圍內**，Owner 自己寫進 merge driver／filter 指令裡的秘密會被顯示與儲存（列入 [[VERIFICATION]] 殘餘風險表）。**2026-08-07 第七次更正（第七輪審查）**：上一句只講了「顯示與儲存」兩處，實際是**三處**——已實測：git 的 trace2 在 merge driver 被呼叫時以 `child_start.argv` 把**完整指令列逐字**寫進這次促進的 `promotion-traces/<id>.jsonl`（`child_class` 是 `"?"` 而不是 `"hook"`，所以它不會出現在 `hooksExecuted` 清單裡，但位元組在檔案裡）。另外，觸發條件不需要 Owner 做任何事：`.git/config` 是 linked worktree 共用的，不可信的 candidate worktree 在核准之前就能寫進 driver／filter 設定。回歸測試刻意使用**非 `credential`** 的 URL 形狀鍵，因為既有測試只打到遮蔽函式的第一臂。回歸測試：`gpg.program`＋`commit.gpgsign`、`core.sshCommand`、`credential.helper`、以及一個 git 裡根本不存在的鍵 `future.someTool.program`，**四者在核准後寫入時都被拒絕且沒有執行**；另一條測試把簽章程式設在**核准之前**（因此沒有漂移可偵測），promotion 成功、該程式**沒有被執行**、產生的 commit 的 `%G?` 為 `N`。**2026-08-06 第四次更正（第四輪審查，本列最嚴重的一次）**：本列從第一版起就寫「並在核准畫面上逐項列出」與（第三次更正）「逐一列在核准畫面上」——**兩句在寫下的當下都是假的**。實測：`public/room.js` 對 `promotion`／`hooks`／`programs`／`configDigest`／`overwrites` 的引用次數**全部為 0**，33 個 `merge-approval-*` DOM 元素裡沒有任何一個是給 hook 清單、設定鍵或被覆蓋的 ignored 路徑用的。資料確實在 payload 裡（`MergeApproval.preview.promotion.hooks`，經 `/api/rooms/merge-approvals/inspect` 送到瀏覽器），**純粹沒有被渲染**——Owner 在打 `MERGE INTO MAIN` 的那一頁看不到會執行什麼。這是 [[PITFALLS]] #77 的形態（未經驗證就把宣稱寫進文件），而且因為 `grantMainMerge` 有 GUI 出口，不適用「第一輪刻意不接出口」的分輪豁免。**現已實作**：`renderMergePromotionDisclosure()` 把 hook 檔名＋SHA-256、`merge.*.driver`、`filter.*`、`programs` 鍵名、`configDigest` 與**這次合併會覆蓋的 ignored／untracked 逐一路徑**渲染在 scroll-gate 量測的那個區域「之內」且在檔案清單之前，因此 Owner 必須捲過它；覆蓋清單改由 `inspectMergeApproval` 每次回傳 live 掃描結果，`checked:false`、缺席、`hooks.unreadable`、以及快照早於閘門四種情況各自是具名阻擋條件（缺席≠空）。已用真實瀏覽器 DOM 驗收，見 [[VERIFICATION]]。 merge 子程序有固定逾時、輸出上限與整個 PGID 的終止（以會掛住的 hook 實測，並斷言 hook 自己的 pid 已消失）。**2026-08-07 第八輪追加的讀取面**：那份 trace 檔現在還被當成「這次 merge 的 process group 是哪一個」的**第三個來源**（`sid` 尾端的 `-P<hex>`），用途只有一個：一列促進紀錄在 SQLite 裡壞掉時，仍然說得出要 Owner 引用哪個號碼。它是唯讀、有界（只讀前 64 KiB 找 `start` 事件）、解析不出來一律回報「不可讀」而不是「沒有 merge」。**2026-08-07 第九輪更正**：上一句漏了這個檔案本身的信任層級。`GIT_TRACE2_EVENT` 的路徑就在 hook 的環境變數裡，而 hook 是本列描述的那個信任邊界——實測 hook 偽造一個 `start` 事件指向死號碼，就讓一列損毀紀錄從「不可讀」變成「已回答」，短短語被接受、排他標記交還，而 `ps -g` 同時列出活著的 merge。**因此這個來源現在只能收緊不能放寬**：它命名的號碼一律拿去探測（只增加拒絕的理由），但永遠不能讓一列變成「已回答」；「有沒有回答」由 row 內的來源決定。另新增第四個來源 `promotion-traces/<id>.spawn-record.json`——產品自己在「記錄 pgid 的那次寫入失敗」時寫下，**路徑不交給任何被觀察的對象**，且它一存在就使該列的「已回答」為 false。同時，讀取這些來源的不再只有釋放路徑：`promotionPending()` 是七條會下結論的路徑共用的閘門，trace 是它的必要參數。**2026-08-07 第十輪更正（本列最根本的一次）**：前九次更正都在調整「哪個來源可信到什麼程度」，而第十輪實測推翻了那整條路線的前提。hook 不需要刪掉任何來源——**它可以供給一個**：registry DB 的路徑是 `dirname(dirname($GIT_TRACE2_EVENT))/candidate-registry.sqlite`，就是產品自己放進 hook 環境的那個變數；而 `mergePromotionHash()` 是**無金鑰 SHA-256** 對公開欄位取值，`#verify()` 又刻意不驗這張表。實測 hook 改寫列內兩個來源一致指向一個已死號碼、重算 `row_hash`、清空 trace、殺掉 owner_pid，得到 `unreadable: false` 的**完全受信任列**，`destructiveOffer`／`notWaiting`／`markerReleased` 三項全部翻轉，產品遞出 `git reset --hard`，而 `ps -g` 同時列著活的 `git merge` 與 `sleep`。**因此本列原先「hash-chained database／hook 碰不到」的說法作廢**：`row_hash` 是完整性偵測不是防偽。**在本列描述的對手模型下，owner uid 寫得到的持久化儲存沒有一個是可信來源。**正向事實只剩兩類：活著的本程序的第一手觀察，與 Owner 親自查看後的宣告；持久紀錄一律只是提示、永遠不是授權。導出的硬規則見 [[VERIFICATION]] 第七處補正 (T-1)(T-2)(T-3)。明寫的代價：orchestrator 在 merge 中途死亡後，產品不再提供自動回滾指令。**2026-08-07 第十一輪（(T) 的實作，本列的信任模型改寫）**：(a) `CONCLUDED_MERGE_REASONS` 由五條縮為三條——`APPROVAL_NEVER_SPENT_NO_GIT_COMMAND_RAN` 與 `MERGE_GROUP_PROBED_GONE` 降級為**提示**（仍具名回報，不再結束等待）。第十輪把後者稱為「正向事實 #3」、把前者稱為「雜湊鏈保護的正向事實 #1」，兩句都作廢：`mergeApprovalHash()` 與 `mergePromotionHash()` 是同一顆無金鑰 SHA-256。**兩者都已實測**：`x1-dbforge forge` 打 #3，新測試 `an approval rewritten to say nothing ever ran…` 打 #1。(b) 新欄位 `MergeWriteConclusion.firstHand`（＝這次呼叫是不是由持有 child handle 的那個程序發出）是破壞性指令的唯一授權；它**沒有持久化途徑**，所以 orchestrator 死過一次之後任何讀取都拿不到 `reset --hard`。(c) `byOwnerDeclaration` 終於有讀取端：Owner 宣告可以結束等待，永不授權破壞性指令——第十輪把這句逐字寫在該欄位的註解裡卻沒有人讀，實測結果是 Owner 照產品印的那句話打完，下一次讀取就拿到 `reset --hard`，而 `ps -g` 同時列著 `git merge`。(d) 兩個**第一手觀察 repository 本身**的狀態仍可自行收斂（`selfEvidencingOutcome()`）：main 的 HEAD 就是那個被授權的雙親 commit，或 main 逐位元回到促進前指紋且無 merge 殘留。這兩個不是「對這件事的宣稱」而是「那個東西本身」，編一列紀錄產生不出來。**hook 仍然可以偽造 Owner 宣告或先前那次結論的位元組**——換到的是「這一列少等一次」，換不到破壞性指令；這一格列在 [[VERIFICATION]] 殘餘風險表。**2026-08-08 第十二輪更正（兩處，都在既有規則之內）**：(a) 第十一輪保留的唯一正向事實**量錯了東西**。`MergeWriteConclusion.firstHand` 的來源是 `ProcessRunner` 的 leader `close`，而 `process-runner.ts:383` 的 `if (terminationReason && processGroupExists()) return;` **只讓被 runner 自己終止的子程序等整個 group**；正常退出只等 leader。實測：hook 只多一行 `( … ) </dev/null >/dev/null 2>&1 &`，`close` 照常觸發，產品遞出 `git reset --hard` 並把排他標記交還，**六秒後那個殘存程序真的往 main 的 tracked 檔案追加了一行**——而同一份 payload 的 `mergeGroupSurvivors` 正具名列著那個活著的 group。修法：欄位改名為 `leaderClosedFirstHand`（名字帶邊界），新增 `MergeIdentityStanding.survivors`（leader 已退出、group 仍有成員），**survivors 非空時不得下結論、不得遞任何指令、不得離開 `applying`**（`applied` 除外，理由與代價見 [[VERIFICATION]] 殘餘風險表）。(b) **`merge.<name>.driver` 是本列描述的信任邊界，但先前有一句殘餘風險把它排除在外**：`selfEvidencingOutcome()` 的 `rolled-back` 豁免以「git 在寫入工作樹之前不執行任何 hook」為由授權，而 driver 不是 hook 且 git 在寫工作樹**之前**執行它（實測：driver 執行當下 HEAD 未動、`diff-index HEAD` 為空、無 `MERGE_HEAD`／`AUTO_MERGE`）。一個 driver 把列內兩來源改寫成「沒有 group」並重算 `row_hash`、清空 trace、`kill -9` owner_pid，就讓一列促進在 `git merge` 仍活著時進入終局 `rolled-back`，**第二次促進因此被放行並真的寫進 main（`mainMutated: true`）**。判準已由缺席（`!trace.spawned`）換成正向事實（本程序向 OS 要過 merge 程序卻從未拿到 pid），跨程序不再豁免。`test/merge-promotion.test.ts`、`test/merge-approval-drift.test.ts`。**2026-08-08 第十三輪更正（三處，都在既有規則之內）**：(a) 第十二輪的 `leaderClosedFirstHand` 量對了東西，**卻是從紀錄裡去找要探測哪個號碼**——`#settlePromotion` 握著 child handle、`onSpawn` 也給過它 pgid，它卻從持久化的號碼重新推導 group。實測：pgid 那次寫入失敗（真實常見，[[PITFALLS]] #65）＋hook 清空 `$GIT_TRACE2_EVENT` 與 `*.spawn-record.json`（**兩者路徑都是本產品親手放進 hook 環境的**）＋一個**不用 `setsid`** 的背景程序，就讓 `survivors` 是空的、紀錄進入終局 `rolled-back`、遞出 `git reset --hard`、第二次促進被放行，而那個殘存程序隨後寫了 main。修法：spawn callback 把 pgid 留在**本呼叫的記憶體變數**裡，settle 與 observation 都用它（[[VERIFICATION]] (V-1)）。(b) **process group 不是「所有還能寫入的程序」的可靠列舉**：`perl -e 'use POSIX; setsid; exec @ARGV'`（macOS 內建）一行就離開，`mergeGroupState()` 先探 leader、leader `gone` 才探 `-pgid`，group 真的空了——實測 leader 第一手退出、group 空、產品遞出 `reset --hard`，六秒後那個程序寫了 main。`grep -rn "setsid\|new session" docs/ src/` 在此之前為零，所以這個維度**不適用「已列出的取捨」**。修法不是再列一種逃脫方式（在沒有沙箱的前提下 OS 不提供「列出逃脫的子孫」的機制），而是改問本列**本來就知道答案**的問題：這次促進有沒有執行過任何 hook 或 merge driver（就是本列為了核准畫面枚舉並雜湊的那份清單，讀不到一律算「有」）。執行過 → leader 退出**不足以**授權破壞性指令，改為具名等待；沒執行過 → 維持原行為。(c) 本列描述的信任邊界在 `applied` 那一格原本有個豁免：survivors 不擋 `applied`。實測那一格的代價不是「殘存程序仍能寫 main」，而是**產品自己的第二次促進會被放行並真的寫進 main**；已改為保留排他標記＋記一個可見狀態，`mainMutated`／`#markCandidateMerged`／audit 的 `mainMutation` 全部改讀 `authorizedMergeCommit` 這個獨立的第一手觀察。**2026-08-09 第十四輪更正（三處，都在既有規則之內）**：(a) 第十三輪把 (V-3) 建立在「產品已經知道會執行哪些 hook」這句話上，**而那句話是假的**。`hookEnvironment()` 用 `git config --list` ＋ `.find(e => e.key === "core.hookspath")` 自己解析——`--list` 印出**每一次出現**、git 生效的是**最後一個**，`find` 取第一個；而且它不展開 `~`。實測：`.git/config` 兩行 `hooksPath`（先 `/dev/null` 後真實目錄），產品回報 `{"hooksPath":"/dev/null","hooks":[],"unreadable":false}`，核准畫面印「本次 promotion 不會執行任何 repo hook」，**而 git 執行了那個 hook**，`setsid` 逃脫程序寫了 main，紀錄進入終局 `rolled-back`＋`reset --hard`、第二次促進被放行；綁定也同時失效（兩份都是空清單）。修法是**直接問 git**：`git rev-parse --git-path hooks`，且在 promotion 會用的那個環境下問，問不出來一律 `unreadable`。**同一個檔案裡其他由 `--list` 取得的鍵已逐一交代**（見 [[VERIFICATION]] 第十四輪的逐鍵表）：`merge.*.driver`／`filter.*`／`programs` 保留列舉並寫明「`--list` 是超集、消費端只會因此多拒絕」，`core.attributesFile` 改用 `config --get --path`（git 自己展開 `~`／`%(prefix)/`）並區分 unset 與展開失敗，手寫的 `expandUserPath()` 已刪除。(b) `untrustedProgramsRan` 是**事前快照**：`restore` 在 `#authorizeMainMerge` 讀，merge 好幾個寫入之後才 spawn，而產品自己 `mkdirSync(promotion-traces)` 就是公開的發令槍。實測：另一個同 uid 程序輪詢到那個目錄出現才寫 `pre-merge-commit`，hook 跑了、`hooksExecuted` 在同一份 payload 裡具名列著它，**隔壁一行是 `reset --hard`**。修法：改為**事前快照 ∪ 事後觀察**，依 (O) 缺席（trace 讀不到或什麼都沒列）一律貢獻 `false`，兩者任一為真即收緊。事後觀察新增 `readExecutedRepositoryPrograms()`，因為本列第七次更正早就記過 `merge.<name>.driver` 的 `child_class` 是 `"?"` 而不是 `"hook"`、**不會**出現在 `hooksExecuted` 裡——所以判準是 `child_class: "hook"` ∨ `use_shell: true` ∨ `argv[0]` 不是 `git`（普通 merge 只啟動 `git stash create` 與 `git maintenance run`，兩者 `argv[0]` 都是 `git`）。**明寫的代價**：核准後才裝、**且同時清空 `$GIT_TRACE2_EVENT`** 的 hook 兩半都看不到，列入 [[VERIFICATION]] 殘餘風險表。(c) `restore.hooks.unreadable` 在 `untrustedProgramsRan` 裡是**不可達的死碼**（同一條件已由 `MAIN_HOOK_DIRECTORY_UNREADABLE` 這個 blocker 直接拒絕促進），已移除並改為斷言那道真正在做事的閘；`MergeIdentityReading.outsideRowOnly`（零讀取端）一併移除。**2026-08-09 第十五輪更正（(X-1)(X-2)(X-3)(X-4)）**：(a) 前十四輪把保護寫成「綁定＋揭露＋消耗前重驗」，而**這三道全部發生在 merge 之前，改變 hook 的正是 merge 本身**。`core.hooksPath = .githooks` 是合法且常見的 plain-git 慣例，它把 hook 目錄放進 **tracked content**；`git merge --no-ff` 先把 candidate 的版本寫進去，再從那裡執行它。實測（純 git 2.50.1 與產品各一次）：核准畫面逐項列出 `sha256 f64801cf…`、git 實際執行 `af872625…`，candidate **只改檔案內容**（exec bit 由 main index 的 `100755` 繼承，不 chmod、不寫 `.git/config`、不必贏任何時序），而 `preview.approvable: true`、`blockers: []`、`promotion state: applied`、`mainMutated: true`，攻擊者程式以 Owner 身分執行完畢。**因此本列與通過標準第 3 項的「在核准畫面上逐項列出本次會執行的 hook 檔名與雜湊」在這個形狀下是假的**；而 `process-runner.ts:203` 那句「hook 不是 tracked content，所以 merge 不可能安裝它自己要執行的 hook」是「允許 promotion 執行 repo 程式」這個開關的載重理由、從未有 probe、實測為假（[[PITFALLS]] #77／#127／#139 同族）。修法不是再加一個雜湊（雜湊描述的是操作前，而操作會改變它），而是問兩個**產品早就有答案**的問題：`git rev-parse --git-path hooks` 回答的目錄是否落在工作樹內（(W-1) 剛做出來的 `insideWorkingTree`），以及本次 merge 的檔案清單（`#overwriteScan` 用的同一份 `preview.files`）是否寫入它；兩者皆是即以 `MERGE_WOULD_INSTALL_THE_HOOKS_IT_RUNS` **在核准之前拒絕**，清單截斷則 `HOOK_DIRECTORY_EXPOSURE_UNVERIFIABLE`。**判準裡沒有 `.githooks` 這個名字**（(X-3) 明文禁止名單思維），所以 `githooks/`、`hooks/`、`tools/hooks/` 與任何 repo 自訂名稱一併涵蓋。刪除與 rename 來源也算——把 Owner 核准的 hook 移走同樣改變了會執行什麼。**退化形照實記，因為兩項實測與審查員的敘述不一致**：`core.hooksPath=""` 時 `git rev-parse --git-path hooks` 回 `./`（產品 join 成工作樹根），但 `git hook run pre-merge-commit` 回 `cannot find a hook named pre-merge-commit`、真實 merge 什麼都沒執行；`core.hooksPath=.` 時 git **會**嘗試並失敗於 `cannot run pre-merge-commit: No such file or directory`。產品在兩種寫法下都拒絕，**比 git 本身嚴格**，這是刻意的取捨而不是對 git 行為的宣稱。(b) **(X-3)**：`src/mcp/workspace-server.ts` 的註解寫著要擋「write-then-auto-execute … git/husky/claude hooks」，實測 `WORKSPACE_SENSITIVE_PATH` 只擋到 `.husky/pre-merge-commit`——`.githooks/pre-merge-commit`／`githooks/…`／`hooks/…`／`tools/hooks/…` 全部 `false`，**Writer 走正常 MCP 就寫得進去**（[[PITFALLS]] #103／#108 第 N 次）。修法同樣是問 git：`WorkspaceToolBroker` 建立時取 `GitBroker.hookDirectoryPosition()`，落在 workspace 內就整個子樹拒絕（`list`／`read`／`create_directory`／`write` 皆拒），**write 模式問不出來一律拒絕建立 broker**（`WORKSPACE_HOOK_DIRECTORY_UNRESOLVED`）。名單保留為縱深防禦，不再是這一類的唯一防線。(c) **(X-4)**：第十四輪明寫的代價「核准後才裝、且同時清空 `$GIT_TRACE2_EVENT` 的 hook 兩半都看不到」，其**終局狀態**比原文重得多——在該攻擊上多加一行「hook 把自己搬走」，得到**第一次促進記成 `rolled-back` ＋ `git … reset --hard`、專案排他標記交還、第二次促進被放行並真的寫了 main，而第一次逃脫的 `setsid` 程序仍在寫**。k2 之所以還被擋，只是因為留在 `.git/hooks` 的檔案讓 `previewDigest` 漂移——偶然，不是設計。修法用的是一個**本程序第一手、被觀察者無法供給**的正向事實：產品自己 spawn git 並握著 pid，而 git 在做任何事之前就把 `version`／`start` 寫進 `GIT_TRACE2_EVENT`（實測），所以「git 起來過」與「trace 裡沒有 top-level `start` 事件」不可能同時為真，除非那次 merge 內部有人清空了它。leader 的 session 以 sid **形狀**辨認（子 git 的 sid 是 `<parent>/<own>`，實測），因此事後補寫的 `start` 補不回答案。這個判準只出現在 `||` 的一側，只能收緊、不能放寬。(d) **本次不修、照實收窄的兩處宣稱**：`readTraceChildren` 只解析 `child_start`／`child_exit`，**不看 git 的 `exec` 事件**；第三條腿是 `basename(argv[0]) !== "git"`，是對檔案**名字**的判斷。兩者都不是已證實的繞過，只是這份觀察看不見的形狀，已寫進註解與 [[VERIFICATION]] 殘餘風險表。**2026-08-09 第七次更正（第十六輪審查）**，兩處，都是把「這一列以為已經關掉的東西」改成實測：(e) **(Y-1)**：上面 (a) 說的 `insideWorkingTree` **不是一個位置，而是一組**。`workingTreePosition()` 的註解寫「兩種拼法取聯集」，實作卻是第一個命中就 `return`，欄位也只是一個字串；**而 merge 寫的是 `realpath` 那一個**。因此 hook 目錄是 symlink 時整道閘被繞過，實測兩種形狀（`core.hooksPath=.githooks` 指向 tracked 的 `.githooks -> tools/hooks`；以及**完全不需要動 config** 的`ln -s ../tools/hooks .git/hooks`）都得到 `approvable: true`、`blockers: []`、**攻擊者程式碼以 Owner 身分執行**、`state: applied`，揭露的 sha256 與實際執行的不同。現在 `insideWorkingTree` 保留 lexical ∪ realpath 全部位置，任一被寫到即擋；上面 (b) 的 MCP 側同步改為 `hookDirectoryPositions()`，**且每次寫入都重新問 git**（`core.hooksPath` 活得比一個 broker 久，建立時問一次是對「現在」這個問題的過期答案）。(f) **(Y-2)**：上面 (c) 的判準**只檢查那份紀錄的內部一致性**，而那份紀錄的路徑（`$GIT_TRACE2_EVENT`）是產品親手交給每一個 hook 的。攻擊者不清空、不偽造、不截斷，改用**過濾**：保留 git 真正的 `version`／`start`（扁平 sid、真 pid），只刪掉 child 事件，三個 disjunct 全滅，終局狀態與修正前的 `k3` 逐項相同（`rolled-back` ＋ `reset --hard` ＋排他標記交還 ＋ 第二次促進寫進 main ＋ `setsid` 殘存程序續寫）。修法不是第四種樣式，而是把判準錨回**本程序記憶體裡的第一手事實**：trace 檔由產品在 spawn git **之前**以 `O_EXCL` 建立並**保留 fd**（`rename(2)` 蓋過路徑不會移動 fd，實測同一次執行下路徑上是乾淨的、fd 上仍具名 `.git/hooks/pre-merge-commit`）；leader 的 session 改用**產品握著的 pid** 比對而不只看 sid 形狀；`readTraceChildren` 不再丟棄沒有配對 `child_start` 的 `child_exit`（那是 in-place 過濾唯一留下的殘跡）。**仍不宣稱窮盡**：這三腿都只能收緊；一個能在 merge 結束後、產品讀取前於**原 inode 上**重寫整份 trace 的殘存程序，仍然是這份觀察看不見的形狀。 | hook 一旦通過綁定就是以 Owner 權限執行的任意程式碼，Orchestratory 不沙箱它、不限制它能碰什麼；同帳號完全失陷仍不在信任邊界內。`.git/config` 可被有終端的 Native agent 直接寫入，因此保護來自「綁定＋揭露＋消耗前重驗」，不是來自阻止寫入 |
| F16 | Provider／API 無限花費 | Owner 選擇 provider、API explicit opt-in、native host controls、usage telemetry | 不使用固定 thread ceiling；provider 自身或 Agent 仍可能持續呼叫 |
| F17 | Runtime 尚未完成但 GUI 宣稱安全／可用 | capability negotiation、pending label、integration tests、truthful docs | 人工驗收仍可能漏掉版本不一致 |
| F18 | 回應不確定的 transport retry 造成重複派工 | 同一 authenticated presence 使用 caller-stable UUID request ID、ledger idempotency、inbox source/request unique binding | MCP host 完全退出後會取得新 presence；跨 host lifetime 的 orphan recovery 仍需後續 stable seat identity／outbox。Caller 若換 ID，系統也不能判斷其意圖相同 |
| F21 | 「本機模型」endpoint 被指到外部主機，成為 prompt／source 外傳通道 | endpoint 預設不註冊、只允許 `127.0.0.1`／`[::1]`／`localhost`（固定成 `127.0.0.1`）＋明確 port、拒絕非 http scheme／帶憑證 URL／path/query/fragment、不跟隨 redirect（3xx 直接失敗）、單次嘗試＋timeout＋輸出 byte 上限、不載入任何 credential、錯誤只回穩定 `LOCAL_*` 代碼 | 同帳號程序仍可在 loopback port 上架設反向代理再往外送；Orchestratory 只保證連線目的地是本機介面，不保證本機那個程序的行為。本機模型輸出仍是不可信內容 |
| F22 | 「無金額成本」被當成「無限制」：免費本機端點上的 agent loop 無節制燒 CPU／RAM／磁碟與時間，或未來新 provider 因政策查表落空而默默繼承免預留 | no-cost 為 provider 明確宣告（`src/providers/billing.ts` 對 `ProviderId` 完整，漏分類即編譯失敗、執行期未宣告 fail closed）；只跳過金額預留，呼叫數（soft/hard＋跨程序 24 小時 governor）、輪數、單次 timeout、workflow 期限、併發、連續失敗、總輸出上限與 kill switch／kill epoch 全數保留；no-cost provider 禁止 `authMode: "api"`；帳目一律記明確 0 與 `billing` 標記 | 本機模型的 CPU／RAM／磁碟用量不受 Orchestratory 配額控制，只受呼叫數與時間上限間接約束；`local` 不佔 `maxSubprocesses` 名額（它不啟動子程序）；本機推論可能長時間佔滿硬體，使用者仍需自行監看主機資源 |
| F27 | 匿名遙測成為新的外傳通道：把路徑、repo 名、帳本、prompt、原始碼、憑證或錯誤訊息全文帶出本機 | **先更正一句這個功能規劃時寫下、而且是錯的前提**：本列原稿寫「這是本產品第一條 provider CLI 以外的網路出口」——實測為假。`src/providers/api.ts:175` 早就有一條程序內 `fetch`，去三個寫死的第一方 LLM endpoint（`api.ts:21/26/31`），由 `authMode === "api"` ＋ 已設定的 API key 兩道開關擋著；`src/providers/local.ts:192` 還有一條，但 `normalizeLocalEndpoint`（`local.ts:45-67`）結構性把它釘在 loopback。遙測真正新的地方不是「第一條出口」，而是**第一條不是為了服務 Owner 剛下的那道指令**的出口——前兩條都由一次明確的模型呼叫觸發，遙測則由「今天第一次啟動」觸發。這是本列存在的理由。緩解分四層：(1) **同意是三態**（`unanswered`/`yes`/`no`，`src/core/telemetry.ts`），只有 `yes` 會送，而且在答覆之前連本機都不累積素材（`recordTelemetryCounter` 在非 `yes` 直接 return）；(2) **欄位白名單是編譯期固定的 schema**——`TelemetryPayload` 的 8 個欄位與 `TELEMETRY_FIELDS` 由型別層 `MutuallyExact` 互相釘死，任一邊多一個欄位 `tsc --noEmit` 就失敗；`telemetryRequestBody()` **不 spread、不列舉輸入的鍵、不對呼叫端的物件呼叫 `JSON.stringify`**，body 是逐個屬性組出來的，因此帶了額外屬性的 payload 貢獻不出任何位元組；(3) **每個字串欄位過既有 redactor**，判準是 `redact(value) !== value` 即拒絕整份回報（不是遮蔽後照送），錯誤代碼另受 `^[A-Z][A-Z0-9_]{2,79}$` 形狀與 `redact` 雙重過濾、不合形狀者逐個丟棄；(4) **出口網域是編譯期常數**（`src/core/telemetry-egress.ts` 的 `TELEMETRY_HOST`／`TELEMETRY_ANON_KEY`），不從設定檔、環境變數、SQLite 或模型輸出取得；`TelemetryTransport` 這個測試替換點刻意**收 body 而不是 URL**，所以沒有任何呼叫端（包含測試）能指定主機。`assertTelemetryUrl` 對 scheme／userinfo／port／host／path／query／fragment 逐項釘死。日期不由客戶端送（伺服器端 `day` 是 generated 欄位），因此連時區都沒有拿到 | **目前尚未布建任何 Supabase project**，`TELEMETRY_HOST` 與 `TELEMETRY_ANON_KEY` 都是 `null`，故本功能**現況連線不到任何地方**——`buildTelemetryRequest` 會丟 `TELEMETRY_ENDPOINT_NOT_PROVISIONED`，`httpsTelemetryTransport` 一律回 `failed`。布建等於改一行原始碼並重新發布，這是刻意的。**已知殘餘風險**：(a) anon key 依設計公開，任何人都能對該 endpoint 灌資料，防灌完全靠伺服器端 `UNIQUE(install_id, day)` ＋ anon 只有 INSERT ——那半邊不在本客戶端的保證範圍內；(b) install id 是 `randomUUID()`（平台 CSPRNG，未從任何機器特徵推導，已由 `telemetry.test.ts` 對原始碼斷言 `node:os` 未被 import、無 `createHash`/`hostname`/`cpus` 等呼叫），但**同一台機器的回報彼此可連結**——這正是「數活躍安裝」需要的性質，也因此它是**假名而非匿名**；(c) 錯誤代碼是全產品 838 個 ad-hoc 字串字面值之一，**沒有中央註冊表**，所以「哪些代碼會被送出」無法由一處審閱，只能靠形狀擋住非代碼內容；一個未來新增、本身就帶識別資訊的大寫代碼會通過所有檢查；(d) 網路失敗一律靜默丟棄且**每天最多嘗試一次**（`lastAttemptedDay`），因此失敗的那一天就永久遺失，這是為了「不得重試堆積」付的價；(e) 同帳號程序可直接改寫 `telemetry.json`（owner-only 0600，但 Full-Trust agent 本來就寫得到），把同意翻成 `yes`——與 F02／F15 同一類，`row_hash` 等完整性保護在本檔案並不存在；(f) 真實 `fetch` 成功路徑**沒有測試覆蓋**，見下方「明確殘餘風險」 |
| F19 | 長駐舊 backend 從開發 repo 現讀新版 Web asset，或中途 migration 讓 DB 同時不相容新舊 runtime | digest-pinned physical release、source daemon install 拒絕、backend/UI protocol、精確 schema fingerprint、交易 migration、WAL-safe DB＋舊 runtime 成對備份 | 同帳號程序仍可修改 release/data；正式切換與 rollback 程序必須逐次驗證 digest、plist、DB integrity 與 ledger receipts |

## 6. STRIDE 摘要

### Spoofing

- 偽造 Owner、terminal seat、provider worker 或 reply source。
- 控制：server-side identity binding、exact presence、session/Room/workspace scope、不可由 payload 覆寫。

### Tampering

- 修改 candidate、main、approval、ledger、audit 或 recovery metadata。
- 控制：Git object IDs、hash/fingerprint、snapshot-bound approval、append-only events、confirm-time recheck。

### Repudiation

- 否認訊息、merge 決定、main 修改或外部副作用。
- 控制：bounded audit 保存 actor、task、thread、candidate/main HEAD、scope、decision 與結果。

### Information Disclosure

- Full-Trust Agent、Room、provider 或 log 暴露 secrets／個資／其他專案。
- 控制：最小 control-plane data、redaction、secret-free audit、清楚的 native-host 信任說明。

### Denial of Service

- 無限活躍 thread、provider loop、process bomb、磁碟填滿或大量 candidate。
- 控制：Owner stop、transport cancellation、resource telemetry、retention/cleanup workflow、provider 原生控制。
- 不以固定 thread 往返輪數處理 DoS，因為它會破壞正常協作。

### Elevation of Privilege

- Orchestratory 偷偷替 Agent 加上或取消 host 權限、GUI Managed policy 污染 Native Full-Trust。
- 控制：mode separation、capability provenance、加入前後能力驗收、禁止 Orchestratory 自動開啟 skip-permissions。

## 7. 必須持續驗證的假設

- Candidate/main canonical path 與 Git identity 能被穩定辨識。
- Provider host 在加入 MCP 前後保持相同的原生能力與批准模型。
- Exact-seat identity 無法由 tool payload 覆寫。
- Main merge approval 能綁定實際 candidate/main snapshot，且 drift 會使其失效。
- Clean/smudge filter 的 attributes 判定仍是 promotion 的安全閘門：除了既有 main 路徑與代表性 probe，
  live gate 也把完整 preview 的每個非刪除目標路徑交給 Git 解析，涵蓋只在 candidate 新出現的路徑；
  preview 不完整或 Git 不可讀時拒絕。這仍不宣稱能防止同帳號 Full-Trust 程序繞過產品直接寫 main。
- Recovery point 可在宣稱成功前實際讀回與驗證。
- Loopback GUI 的 session、Origin、Host 與 CSRF 防護有效。
- Runtime／GUI／文件能透過版本或 capability negotiation 誠實顯示已實作功能。

## 8. 明確殘餘風險

- Full-Trust Agent 能讀寫整台 Mac、執行任意程式並使用網路；若失控，應用層無法提供強隔離。
- 行為規範能約束合規 Agent，但不能阻止惡意或被完全攻陷的同帳號程序。
- Git worktree／clone 與 candidate 不等同 VM；錯誤 Git 指令仍可能影響共用 object database 或 refs。
- Snapshot、Time Machine 與外部備份的實際可恢復性取決於系統設定，必須測試後才能宣稱。
- 不設 thread 回合上限會保留長期資源消耗風險；以使用者停止、provider controls、telemetry 與 retention
  管理，而不是硬切斷協作。
- Provider 與遠端服務會處理收到的內容；本機 Orchestratory 無法消除其政策與基礎設施風險。
- Main merge 以外的直接 host 操作仍依賴 Agent 規範與 provider 自有 permission UX。
- Candidate 半建立列的回收依賴 `owner_pid` 這個**非持久**的 liveness 判準（見 F20）；pid 重用（重開機後 pid 從低號重啟，而重開機正是最常造成保留被遺棄的原因）或跨主機共享資料目錄都會使它判斷錯誤；兩個方向的後果分別是「永不解析」與「重複 candidate」，皆不影響 canonical main 內容。設計上偏好前者，因為它可見且可逆。
- Candidate mutation 已具 durable request idempotency（見 F15）。殘餘限制：記憶體鎖只涵蓋單一程序，
  跨 process 的同 key 併發由 reservation 的不透明 `owner_token` 擋下——**每一個對 `candidate_requests` 的寫入都必須帶 token**（結構性不變式，非逐呼叫點檢查），輸家會收到錯誤而非排隊；
  暫時性 Git 失敗**不再燒掉該 key**（終局 `failed` 改由 7 個確定性錯誤碼的 allowlist 決定，其餘視為
  結果未知並維持 `pending`）。新的殘餘：確定性但未列入 allowlist 的失敗會讓該列永久停在 `pending`。
  孤兒 recovery ref 已有唯讀 CLI 出口 `orchestrator candidates orphan-refs <workspace>`（只列不刪）。
- Promotion 紀錄自 2026-08-07 起有 CLI 列出／人工釋放出口；2026-08-14 起另有本機 Owner HTTP
  grant＋promotion 出口與 GUI Merge 歷史。兩種列出都會重新觀察並可能更新未結紀錄，但不會重跑 merge；
  三種 release 不殺程序、不寫 main、不決定結果。Agent MCP 仍沒有 promote 權限。
- Dirty-state 完整內容雜湊每次 inspection 共用 30 秒 deadline；極大或極慢檔案會 fail closed，要求 Owner
  處理工作區狀態後重試，不會以 partial fingerprint 冒充穩定 snapshot。
- 遙測（F27）的真實 `fetch` **成功路徑沒有測試覆蓋**。要覆蓋它只有兩條路：真的連出去（本輪明文禁止），
  或讓目的地變成可注入的參數（那正是「出口網域必須寫死在程式碼裡」要排除的東西）。因此被測到的是
  `httpsTelemetryTransport` 的**拒絕分支**與 `assertTelemetryUrlAgainst` 的逐項釘死，送出成功那一段
  只有在 Owner 布建 project 之後、由人親自驗一次才算驗過。這是刻意的取捨，不是遺漏。
- 遙測的 `ran_today`／`promotions`／`apply_backs` 由三個呼叫點餵養（`workflow.ts` 的 `workflow.started`、
  `collaboration-service.ts` 的 `phase === "settled" && state === "applied"`、`apply-back-broker.ts` 的
  成功 emit 之後）。三者都是 `void`、都不會拋、也都不進入各自操作的結果，因此**遙測記帳失敗不會改變
  任何一次 promotion 或 apply-back 的結局**；代價是計數可能少計（寫入失敗即無聲丟棄），
  它是粗略指標而不是稽核來源——稽核仍以 `events` 雜湊鏈與 `collaboration_audit` 為準。
- 遙測的同意狀態**每次讀取都回磁碟**，不快取在 `AppContext`。這是刻意與 `app.retention` 相反的選擇：
  後者是啟動時的凍結快照，因此在 GUI 改了之後執行中的程序仍用舊值；同意若照抄那個模式，
  就會出現「同一句話在 GUI 與 TUI 不一致」。
